class Main {
    static int x;

    static int main(Array<String> args) {
        x = 123;
        printX();
    }

    static void printX() {
        var p = new Printer(new File(1));
        p.printInt<int>(x);
        p.newLine();
    }

    static int main3(Array<String> args) {
        var arr = new ArrayList<int>();
        for (var i = 0; i < 10; i = i + 1) {
            arr.push(i);
        }
        var p = new Printer(new File(1));
        p.printInt<long>(arr.length);
        p.newLine();
        p.printInt<long>(arr._array.length);
        p.newLine();
        p.newLine();
        for (var i = 0; i < arr.length; i = i + 1) {
            p.printInt<int>(arr.get(i));
            p.newLine();
        }
    }

    static int main2(Array<String> args) {
        var f = File.open(args[0], 0);
        var stdout = new File(1);
        ulong bufsz = 4096;
        var buf = new Byte[bufsz];
        ulong nread;
        var p = new Printer(stdout);
        while ((nread = f.read(buf, bufsz)) != 0) {
            for (var i = 0; i < nread; i = i + 1) {
                p.printInt<byte>(buf[i]);
                p.newLine();
            }
        }
        f.close();
        delete f;
        delete stdout;
        delete buf;
        return 0;
    }
}
